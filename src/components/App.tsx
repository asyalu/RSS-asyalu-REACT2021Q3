import React, { useState } from 'react';
import '../styles/App.scss';
import Page from './Page';

const App = (): JSX.Element => (
  <main className="main">
    <Page />
  </main>
);

export default App;
